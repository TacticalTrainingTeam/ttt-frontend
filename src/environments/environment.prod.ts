export const environment = {
    production: true,
    apiBaseUrl: '/api/v1',
    // Serve dummy data when the backend is unreachable; set to false once the API is live
    useDummyFallback: true,
};
